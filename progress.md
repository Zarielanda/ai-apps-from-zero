# 学习进度与复盘

> 每周结束后复制一份下面的模板，保留真实记录；不要只记录“学完了什么”。

## Week 1

- [x] Day 1
- [ ] Day 2
- [ ] Day 3
- [ ] Day 4
- [ ] Day 5
- [ ] Day 6
- [ ] Day 7 复盘

### 本周学到的三个概念

1. 终端命令由命令和参数组成；`cd` 用于切换目录，`mkdir` 用于创建目录。
2. Python 的 `input()` 接收用户输入，`print()` 将内容显示在终端。
3. f-string 可通过 `{变量名}` 将变量值插入输出文字。

### 我遇到的报错 / 问题及解决方法

| 问题 | 原因 | 如何解决 | 下次如何避免 |
| --- | --- | --- | --- |
| `mkdir src` 提示无权限 | 当时位于受保护的 `C:\Windows\System32` 目录 | 使用 `cd` 进入课程项目目录后再操作 | 先阅读提示符中的当前路径 |
| `SyntaxError: unterminated string literal` | 删除了字符串末尾的英文双引号 | 补回缺失的英文直引号并重新运行 | 每个开引号都检查是否有匹配的结尾引号 |

### 下周需要调整的地方

- 
